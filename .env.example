# ============================================================
# Platform Status Dashboard — Environment Variables
# Copy this to .env.local and fill in values
# ============================================================

# ─── Background Scheduler ──────────────────────────────────
# How often the in-process scheduler runs (minutes).
# This is IN ADDITION to the system cron job — both can run.
# The cron job is recommended as the primary checker.
CRON_INTERVAL_MINUTES=2

# Enable in-process scheduler in dev (auto-enabled in production)
# ENABLE_SCHEDULER=true

# ─── Slack Alerts (optional) ────────────────────────────────
# Get webhook URL from: https://api.slack.com/messaging/webhooks
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ─── Email Alerts (SMTP — optional) ────────────────────────
# Works with Gmail, Outlook, SendGrid, AWS SES, etc.
#
# Gmail setup:
#   1. Enable 2FA → https://myaccount.google.com/security
#   2. Create App Password → https://myaccount.google.com/apppasswords
#   3. Use the 16-char app password as SMTP_PASS
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-char-app-password
# ALERT_EMAIL_FROM=your-email@gmail.com
# ALERT_EMAIL_TO=admin@example.com,devops@example.com

# ─── Alert Settings ────────────────────────────────────────
# Minutes between repeated alerts for the same service
ALERT_COOLDOWN_MINUTES=10


